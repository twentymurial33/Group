

   
    <link rel="stylesheet" type="text/css" href="assets\css\style.css">
    <script src='assets/js/yardsale.js'></script>
    <script src='assets/js/getproducts.js'></script>
    <title>Buyer</title>
<body background="assets\images\spiration-light.png" color=aquamarin>
    <nav class="navbar">
        <div class="container-fluid">
            <form class="navbar-form navbar-left">
                <a class="postStuff" href="#">All the things!</a>
                <button type="button" class="btn btn-default navbar-btn" id="buyBtn">Buy</button>
          <a href="/seller"><button type="button" class="btn btn-default navbar-btn" id="sellBtn">Sell</button></a>
                <div class="form-group">
                    <input type="text" class="form-control" placeholder="Search" id="sellersearch">
                </div>
                <button type="submit" class="btn btn-default navbar-btn" id="submitBtn">Submit</button>
                <button type="button" class="btn btn-default navbar-btn" id="signinBtn">Sign in</button>
                <a class="sellStuff" href="#">Treat Yourself!</a>
            </form>
           </div>
    </nav>
     <label for="category">Select Category:</label>
           <p>{{newItem}}</p>
        {{#each product}}
	<tr>
		<td>{{ seller_name }}</td>
		<td>{{ product_name }}</td>
        <td>{{ product_description }}</td>
        <td>{{ category }}</td>
        <td>{{price}}</td>
        <td>{{img_url}}</td>
	</tr>
{{/each}}
    <div class = 'container-fluid'>
        <select class="form-control col-md-8" id="category">
            <option value="clothing" id="clothing">Clothing</option>
            <option value="furniture" id="furniture">Furniture</option>
            <option value="sportingGoods" id="sporting">Sporting Goods</option>
            <option value="sportingGoods" id="household">Household Items</option>
            <option value="misc" id="misc">Misc</option>
        </select>
        <button class = 'btn btn-default col-md-offset-1' id = 'find-items'>Find!</button>
        <button class = 'btn btn-default' id = 'get-items'>Get All Items</button>
        <br>
    </div>
    <div class = 'container'>
        <div id = 'results-field' class = 'row'>

        </div>
    </div>
</body>
